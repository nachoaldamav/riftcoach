{
  "name": "riftcoach",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "pnpm run -r compile",
    "export:mongodb": "pnpm run -r --filter @riftcoach/apps.mongodb-exporter export",
    "export:pro": "pnpm run -r --filter @riftcoach/apps.pro-exporter export",
    "dev:api": "pnpm run -r --filter api dev",
    "build:api": "pnpm run -r --filter api build",
    "start:api": "pnpm run -r --filter api start",
    "dev:web": "pnpm run -r --filter web dev",
    "build:web": "pnpm run -r --filter web build",
    "start:web": "pnpm run -r --filter web start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.14.0",
  "dependencies": {
    "@aws-sdk/client-athena": "*",
    "sst": "3.17.13"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/aws-lambda": "8.10.152",
    "tsx": "^4.20.5"
  }
}